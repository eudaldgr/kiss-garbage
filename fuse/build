#!/bin/sh -e

export DESTDIR="$1"

patch -p1 < fix-realpath.patch

meson \
	--prefix=/usr \
	--sysconfdir=/etc \
	--mandir=/usr/share/man \
	--localstatedir=/var \
	--buildtype=plain \
	-Duseroot=false \
	-Dexamples=false \
	. output

ninja -C output
ninja -C output	install

rm -rf "$1/etc/init.d/"
